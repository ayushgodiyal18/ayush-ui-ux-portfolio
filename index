<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Ayush Godiyal | UI/UX Portfolio</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header class="navbar">
    <div class="logo">Ayush.</div>
    <nav>*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}
:root{
  --bg:#0f172a;
  --card:#111827;
  --text:#f8fafc;
  --muted:#94a3b8;
  --primary:#7c3aed;
  --secondary:#06b6d4;
}
body{
  font-family:'Poppins',sans-serif;
  background:radial-gradient(circle at top right,#1e293b,#0f172a 60%);
  color:var(--text);
  line-height:1.6;
}
.navbar{
  position:sticky;
  top:0;
  z-index:100;
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:1rem 8%;
  background:rgba(15,23,42,.7);
  backdrop-filter:blur(10px);
  border-bottom:1px solid rgba(255,255,255,.08);
}
.logo{
  font-size:1.5rem;
  font-weight:700;
  color:#fff;
}
nav{
  display:flex;
  gap:1.25rem;
}
nav a{
  color:var(--text);
  text-decoration:none;
  font-weight:500;
  transition:.3s;
}
nav a:hover{
  color:var(--secondary);
}
.hero{
  min-height:100vh;
  display:grid;
  grid-template-columns:1.2fr .8fr;
  align-items:center;
  gap:3rem;
  padding:4rem 8%;
}
.tag{
  color:var(--secondary);
  font-weight:600;
  margin-bottom:1rem;
}
.hero h1{
  font-size:3.4rem;
  line-height:1.1;
  margin-bottom:1rem;
}
.hero h1 span{
  background:linear-gradient(90deg,var(--primary),var(--secondary));
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}
.subtitle{
  color:var(--muted);
  max-width:600px;
}
.hero-buttons{
  margin-top:2rem;
  display:flex;
  gap:1rem;
  flex-wrap:wrap;
}
.btn{
  padding:.9rem 1.5rem;
  border-radius:999px;
  text-decoration:none;
  font-weight:600;
  transition:.3s;
}
.primary{
  background:linear-gradient(135deg,var(--primary),var(--secondary));
  color:#fff;
}
.secondary{
  border:1px solid rgba(255,255,255,.15);
  color:#fff;
}
.btn:hover{
  transform:translateY(-2px);
}
.hero-image{
  display:flex;
  justify-content:center;
}
.hero-image img{
  width:320px;
  height:320px;
  border-radius:50%;
  object-fit:cover;
  border:6px solid rgba(255,255,255,.08);
  box-shadow:0 20px 50px rgba(0,0,0,.45);
}
.section{
  padding:5rem 8%;
}
.section h2{
  font-size:2rem;
  margin-bottom:2rem;
}
.card,.contact-box{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:20px;
  padding:1.5rem;
  backdrop-filter:blur(10px);
}
.skills-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(180px,1fr));
  gap:1rem;
}
.skill-card{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:18px;
  padding:1.25rem;
  text-align:center;
  font-weight:600;
  transition:.3s;
}
.skill-card:hover{
  transform:translateY(-5px);
  border-color:rgba(6,182,212,.6);
}
.projects-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:1.5rem;
}
.project-card{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:22px;
  overflow:hidden;
  transition:.3s;
}
.project-card:hover{
  transform:translateY(-6px);
  box-shadow:0 20px 40px rgba(0,0,0,.35);
}
.project-card img{
  width:100%;
  height:180px;
  object-fit:cover;
}
.project-card h3{
  padding:1rem 1rem .25rem;
}
.project-card p{
  padding:0 1rem 1.25rem;
  color:var(--muted);
}
footer{
  text-align:center;
  padding:2rem;
  color:var(--muted);
}
@media(max-width:860px){
  .hero{
    grid-template-columns:1fr;
    text-align:center;
  }
  .hero-buttons{
    justify-content:center;
  }
  nav{
    gap:.75rem;
    font-size:.95rem;
  }
}

      <a href="#home">Home</a>
      <a href="#about">About</a>
      <a href="#skills">Skills</a>
      <a href="#projects">Projects</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <section id="home" class="hero">
    <div class="hero-text">
      <p class="tag">UI/UX Designer & Frontend Developer</p>
      <h1>Hi, I'm <span>Ayush Godiyal</span></h1>
      <p class="subtitle">I design modern interfaces, build responsive websites, and turn ideas into clean digital experiences.</p>
      <div class="hero-buttons">
        <a class="btn primary" href="#projects">View Projects</a>
        <a class="btn secondary" href="#contact">Hire Me</a>
      </div>
    </div>
    <div class="hero-image">
      <img src="assets/images/profile.png" alt="Profile">
    </div>
  </section>

  <section id="about" class="section">
    <h2>About Me</h2>
    <div class="card">
      <p>I'm a BCA graduate and aspiring UI/UX designer from Uttarakhand. I enjoy creating clean interfaces, interactive prototypes, and responsive web experiences using Figma, HTML, CSS, and JavaScript.</p>
    </div>
  </section>

  <section id="skills" class="section">
    <h2>Skills</h2>
    <div class="skills-grid">
      <div class="skill-card">Figma</div>
      <div class="skill-card">HTML5</div>
      <div class="skill-card">CSS3</div>
      <div class="skill-card">JavaScript</div>
      <div class="skill-card">Responsive Design</div>
      <div class="skill-card">Prototyping</div>
    </div>
  </section>

  <section id="projects" class="section">
    <h2>Projects</h2>
    <div class="projects-grid">
      <div class="project-card">
        <img src="assets/images/project1.png" alt="Pizza Website">
        <h3>Pizza Website UI</h3>
        <p>Modern restaurant landing page designed in Figma and built with HTML/CSS.</p>
      </div>
      <div class="project-card">
        <img src="assets/images/project2.png" alt="Dashboard">
        <h3>SaaS Dashboard</h3>
        <p>Clean admin dashboard with analytics cards and responsive layout.</p>
      </div>
      <div class="project-card">
        <img src="assets/images/project3.png" alt="E-commerce">
        <h3>E-commerce Store</h3>
        <p>Responsive shopping interface with product cards and smooth interactions.</p>
      </div>
    </div>
  </section>
const sections=document.querySelectorAll('.section, .hero');
const observer=new IntersectionObserver((entries)=>{
  entries.forEach(entry=>{
    if(entry.isIntersecting){
      entry.target.classList.add('show');
    }
  });
},{threshold:0.15});
sections.forEach(s=>observer.observe(s));

document.querySelectorAll('nav a').forEach(link=>{
  link.addEventListener('click',e=>{
    e.preventDefault();
    document.querySelector(link.getAttribute('href'))
      .scrollIntoView({behavior:'smooth'});
  });
});

*{
  margin:0;
  padding:0;
  box-sizing:border-box;
}
:root{
  --bg:#0f172a;
  --card:#111827;
  --text:#f8fafc;
  --muted:#94a3b8;
  --primary:#7c3aed;
  --secondary:#06b6d4;
}
body{
  font-family:'Poppins',sans-serif;
  background:radial-gradient(circle at top right,#1e293b,#0f172a 60%);
  color:var(--text);
  line-height:1.6;
}
.navbar{
  position:sticky;
  top:0;
  z-index:100;
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:1rem 8%;
  background:rgba(15,23,42,.7);
  backdrop-filter:blur(10px);
  border-bottom:1px solid rgba(255,255,255,.08);
}
.logo{
  font-size:1.5rem;
  font-weight:700;
  color:#fff;
}
nav{
  display:flex;
  gap:1.25rem;
}
nav a{
  color:var(--text);
  text-decoration:none;
  font-weight:500;
  transition:.3s;
}
nav a:hover{
  color:var(--secondary);
}
.hero{
  min-height:100vh;
  display:grid;
  grid-template-columns:1.2fr .8fr;
  align-items:center;
  gap:3rem;
  padding:4rem 8%;
}
.tag{
  color:var(--secondary);
  font-weight:600;
  margin-bottom:1rem;
}
.hero h1{
  font-size:3.4rem;
  line-height:1.1;
  margin-bottom:1rem;
}
.hero h1 span{
  background:linear-gradient(90deg,var(--primary),var(--secondary));
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}
.subtitle{
  color:var(--muted);
  max-width:600px;
}
.hero-buttons{
  margin-top:2rem;
  display:flex;
  gap:1rem;
  flex-wrap:wrap;
}
.btn{
  padding:.9rem 1.5rem;
  border-radius:999px;
  text-decoration:none;
  font-weight:600;
  transition:.3s;
}
.primary{
  background:linear-gradient(135deg,var(--primary),var(--secondary));
  color:#fff;
}
.secondary{
  border:1px solid rgba(255,255,255,.15);
  color:#fff;
}
.btn:hover{
  transform:translateY(-2px);
}
.hero-image{
  display:flex;
  justify-content:center;
}
.hero-image img{
  width:320px;
  height:320px;
  border-radius:50%;
  object-fit:cover;
  border:6px solid rgba(255,255,255,.08);
  box-shadow:0 20px 50px rgba(0,0,0,.45);
}
.section{
  padding:5rem 8%;
}
.section h2{
  font-size:2rem;
  margin-bottom:2rem;
}
.card,.contact-box{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:20px;
  padding:1.5rem;
  backdrop-filter:blur(10px);
}
.skills-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(180px,1fr));
  gap:1rem;
}
.skill-card{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:18px;
  padding:1.25rem;
  text-align:center;
  font-weight:600;
  transition:.3s;
}
.skill-card:hover{
  transform:translateY(-5px);
  border-color:rgba(6,182,212,.6);
}
.projects-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:1.5rem;
}
.project-card{
  background:rgba(17,24,39,.7);
  border:1px solid rgba(255,255,255,.08);
  border-radius:22px;
  overflow:hidden;
  transition:.3s;
}
.project-card:hover{
  transform:translateY(-6px);
  box-shadow:0 20px 40px rgba(0,0,0,.35);
}
.project-card img{
  width:100%;
  height:180px;
  object-fit:cover;
}
.project-card h3{
  padding:1rem 1rem .25rem;
}
.project-card p{
  padding:0 1rem 1.25rem;
  color:var(--muted);
}
footer{
  text-align:center;
  padding:2rem;
  color:var(--muted);
}
@media(max-width:860px){
  .hero{
    grid-template-columns:1fr;
    text-align:center;
  }
  .hero-buttons{
    justify-content:center;
  }
  nav{
    gap:.75rem;
    font-size:.95rem;
  }
}

  <section id="contact" class="section">
    <h2>Contact</h2>
    <div class="contact-box">
      <p><strong>Email:</strong> ayush@example.com</p>
      <p><strong>LinkedIn:</strong> linkedin.com/in/yourprofile</p>
      <p><strong>GitHub:</strong> github.com/yourusername</p>
    </div>
  </section>

  <footer>
    <p>© 2026 Ayush Godiyal • Designed with ❤️</p>
  </footer>

  <script src="script.js"></script>
</body>
</html>

